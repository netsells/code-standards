{
  "private": true,
  "scripts": {
    "build": "yarn generate && vuepress build src",
    "dev": "yarn generate && vuepress dev src",
    "generate": "yarn generate:linters",
    "generate:linters": "node ./.build/generate-linter-pages.js",
    "lerna:publish": "lerna publish",
    "lerna": "lerna",
    "test": "lerna run test"
  },
  "dependencies": {
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    "@vuepress/plugin-google-analytics": "^2.0.0-beta.26",
    "@vuepress/plugin-register-components": "^2.0.0-beta.26",
    "@vuepress/plugin-search": "^2.0.0-beta.26",
    "fs-extra": "^8.0.1",
    "husky": "^2.3.0",
    "lerna": "^3.14.1",
    "vuepress": "^2.0.0-beta.26"
  }
}
