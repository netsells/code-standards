// 2 space indent
const b = () => {
  return {
    foo: 'bar',
  };
};

// 8 space indent
const c = () => {
        return {
                foo: 'bar',
        };
};

    // 4 space initial indent
    const d = () => {
        return {
            foo: 'bar',
        };
    };

// spacing using the fibonacci sequence
const d = () => {
 return {
   foo: {
      bar: {
           hello: {
                   world: null,
           },
      },
   },
 };
};
