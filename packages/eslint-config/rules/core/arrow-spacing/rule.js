const _THROW = require('../../../modules/throwables');

module.exports = {
    rules: {
        'arrow-spacing': [
_THROW.WARNING,
{
            before: true,
            after: true,
        },
],
    },
};
